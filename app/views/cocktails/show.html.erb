<h1><%= @cocktail.name %></h1>

<h2>Ingredients :</h2>
 <% @cocktail.doses.each do |dose| %>
  <ul>
    <li><%= dose.ingredient.name %> // Description : <%= dose.description %> <%= link_to icon('fas', 'trash'), dose_path(dose), method: :delete, data: {confirm: "Delete #{dose.ingredient.name}"} %></li>
  </ul>
<% end %>
<br>

<%= link_to 'Back to the list of cocktail', cocktails_path %> |

<%= link_to 'Add description and ingredient', new_cocktail_dose_path(@cocktail) %> |
