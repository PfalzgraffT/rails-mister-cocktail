<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>

  <%= f.submit "Submit" %>
<% end %>

<%= link_to 'Back to the cocktail info', cocktail_path(@cocktail) %>
